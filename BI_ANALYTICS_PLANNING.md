# خطة تطوير نظام التحليلات والتقارير المتقدمة
## Symbol AI ERP - Business Intelligence & AI Analytics

**التاريخ:** 6 يناير 2026  
**المُعد:** Manus AI  
**الإصدار:** 1.0

---

## المقدمة

يهدف هذا المستند إلى تقديم خطة تفصيلية شاملة لتطوير ثلاثة مكونات رئيسية في نظام Symbol AI ERP:

1. **لوحة تحكم BI متقدمة** (Advanced BI Dashboard)
2. **منشئ التقارير المخصصة** (Custom Report Builder)
3. **تحليلات الذكاء الاصطناعي** (AI-Powered Analytics)

---

# الجزء الأول: لوحة تحكم BI المتقدمة

## 1.1 الرؤية والأهداف

### الرؤية
إنشاء لوحة تحكم تفاعلية شاملة توفر رؤية 360 درجة لأداء الأعمال، مع إمكانية التخصيص الكامل والتحليل العميق للبيانات.

### الأهداف الرئيسية

| الهدف | المقياس | المستهدف |
|-------|---------|----------|
| تحسين سرعة اتخاذ القرار | وقت الوصول للمعلومة | < 3 ثواني |
| زيادة استخدام البيانات | نسبة المستخدمين النشطين | > 80% |
| تقليل الاعتماد على IT | طلبات التقارير اليدوية | -70% |
| تحسين دقة التوقعات | نسبة الخطأ في التوقعات | < 10% |

## 1.2 مصادر البيانات المتاحة

### جداول البيانات الأساسية (59 جدول)

| الفئة | الجداول | البيانات المتاحة |
|-------|---------|------------------|
| **المبيعات** | invoices, invoiceItems, dailyRevenues, employeeRevenues | الفواتير، الإيرادات اليومية، أداء الموظفين |
| **المشتريات** | purchaseOrders, purchaseOrderItems, suppliers | أوامر الشراء، الموردين، التكاليف |
| **المخزون** | products, productBatches, inventoryMovements, inventoryCounts | المنتجات، الدفعات، حركة المخزون |
| **المالية** | expenses, payrolls, payrollDetails, financialKpis | المصاريف، الرواتب، المؤشرات المالية |
| **الموارد البشرية** | employees, employeeRequests, weeklyBonuses | الموظفين، الطلبات، البونص |
| **العملاء** | customers, loyaltyCustomers, loyaltyVisits | العملاء، برنامج الولاء |
| **الفروع** | branches, monthlyRecords | الفروع، السجلات الشهرية |

## 1.3 هيكل لوحة التحكم

### 1.3.1 الصفحة الرئيسية (Executive Overview)

```
┌─────────────────────────────────────────────────────────────────┐
│  🎯 ملخص الأداء اليومي                    📅 اليوم | الأسبوع | الشهر │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ المبيعات │  │ المصاريف │  │ الأرباح  │  │ العملاء  │        │
│  │  ↑ 15%   │  │  ↓ 5%    │  │  ↑ 20%   │  │  ↑ 8%    │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
├─────────────────────────────────────────────────────────────────┤
│  📊 رسم بياني تفاعلي للمبيعات vs المصاريف (آخر 30 يوم)         │
│  [Area Chart with Comparison]                                   │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────┐  ┌─────────────────────┐              │
│  │ 🏆 أفضل الفروع      │  │ 👥 أفضل الموظفين    │              │
│  │ 1. فرع الرياض      │  │ 1. أحمد محمد        │              │
│  │ 2. فرع جدة         │  │ 2. سارة علي         │              │
│  └─────────────────────┘  └─────────────────────┘              │
├─────────────────────────────────────────────────────────────────┤
│  ⚠️ التنبيهات الذكية                                           │
│  • 5 منتجات وصلت للحد الأدنى                                   │
│  • انخفاض مبيعات فرع الدمام 20%                                │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3.2 لوحة المبيعات (Sales Dashboard)

**المكونات:**

1. **مؤشرات الأداء الرئيسية (KPIs)**
   - إجمالي المبيعات (يومي/أسبوعي/شهري/سنوي)
   - متوسط قيمة الفاتورة
   - عدد الفواتير
   - معدل النمو مقارنة بالفترة السابقة

2. **الرسوم البيانية**
   - خط زمني للمبيعات (Line Chart)
   - توزيع المبيعات حسب الفرع (Pie Chart)
   - مقارنة الفروع (Bar Chart)
   - خريطة حرارية للمبيعات حسب الساعة/اليوم (Heatmap)

3. **جداول تفاعلية**
   - أفضل 10 منتجات مبيعًا
   - أفضل 10 عملاء
   - أداء الموظفين

### 1.3.3 لوحة المخزون (Inventory Dashboard)

**المكونات:**

1. **مؤشرات المخزون**
   - قيمة المخزون الإجمالية
   - معدل دوران المخزون
   - المنتجات منخفضة المخزون
   - المنتجات قريبة الانتهاء

2. **تحليل ABC**
   - تصنيف المنتجات (A: 80% من القيمة، B: 15%، C: 5%)
   - توصيات إعادة الطلب

3. **تنبؤات المخزون**
   - توقع نفاد المخزون
   - اقتراحات كميات الطلب المثلى

### 1.3.4 لوحة الموارد البشرية (HR Dashboard)

**المكونات:**

1. **إحصائيات الموظفين**
   - عدد الموظفين حسب الفرع
   - معدل الحضور
   - إجمالي الرواتب

2. **أداء الموظفين**
   - ترتيب الموظفين حسب الإيرادات
   - البونص المستحق
   - الطلبات المعلقة

### 1.3.5 لوحة المالية (Financial Dashboard)

**المكونات:**

1. **قائمة الدخل المختصرة**
   - الإيرادات
   - تكلفة المبيعات
   - المصاريف التشغيلية
   - صافي الربح

2. **مؤشرات الربحية**
   - هامش الربح الإجمالي
   - هامش الربح الصافي
   - العائد على الاستثمار (ROI)

3. **التدفقات النقدية**
   - المقبوضات vs المدفوعات
   - الرصيد النقدي المتوقع

## 1.4 المتطلبات التقنية

### 1.4.1 المكتبات والأدوات

| المكتبة | الاستخدام | السبب |
|---------|-----------|-------|
| **Recharts** (موجود) | الرسوم البيانية الأساسية | سهولة الاستخدام مع React |
| **Apache ECharts** | رسوم بيانية متقدمة | دعم Heatmaps و 3D |
| **TanStack Table** | جداول تفاعلية | Sorting, Filtering, Pagination |
| **React Grid Layout** | تخطيط قابل للسحب | تخصيص لوحة التحكم |
| **date-fns** (موجود) | معالجة التواريخ | خفيف وسريع |

### 1.4.2 هيكل الملفات المقترح

```
client/src/
├── pages/
│   └── BIDashboard/
│       ├── index.tsx              # الصفحة الرئيسية
│       ├── SalesDashboard.tsx     # لوحة المبيعات
│       ├── InventoryDashboard.tsx # لوحة المخزون
│       ├── HRDashboard.tsx        # لوحة الموارد البشرية
│       └── FinancialDashboard.tsx # لوحة المالية
├── components/
│   └── bi/
│       ├── KPICard.tsx            # بطاقة المؤشر
│       ├── ChartContainer.tsx     # حاوية الرسم البياني
│       ├── DataTable.tsx          # جدول البيانات
│       ├── FilterPanel.tsx        # لوحة الفلاتر
│       ├── DateRangePicker.tsx    # محدد نطاق التاريخ
│       ├── BranchSelector.tsx     # محدد الفرع
│       └── ExportButton.tsx       # زر التصدير
└── hooks/
    └── bi/
        ├── useSalesData.ts        # بيانات المبيعات
        ├── useInventoryData.ts    # بيانات المخزون
        └── useFinancialData.ts    # بيانات المالية
```

### 1.4.3 API Endpoints المطلوبة

```typescript
// server/routers/bi.router.ts

biRouter = {
  // ملخص تنفيذي
  getExecutiveSummary: publicProcedure
    .input(z.object({ 
      dateRange: z.enum(['day', 'week', 'month', 'year']),
      branchId: z.number().optional()
    }))
    .query(async ({ input }) => { ... }),

  // بيانات المبيعات
  getSalesAnalytics: publicProcedure
    .input(z.object({
      startDate: z.date(),
      endDate: z.date(),
      branchId: z.number().optional(),
      groupBy: z.enum(['day', 'week', 'month'])
    }))
    .query(async ({ input }) => { ... }),

  // تحليل المخزون
  getInventoryAnalytics: publicProcedure
    .input(z.object({
      branchId: z.number().optional(),
      classification: z.enum(['all', 'A', 'B', 'C']).optional()
    }))
    .query(async ({ input }) => { ... }),

  // مؤشرات مالية
  getFinancialKPIs: publicProcedure
    .input(z.object({
      startDate: z.date(),
      endDate: z.date()
    }))
    .query(async ({ input }) => { ... }),

  // مقارنة الفروع
  getBranchComparison: publicProcedure
    .input(z.object({
      startDate: z.date(),
      endDate: z.date(),
      metric: z.enum(['sales', 'expenses', 'profit', 'customers'])
    }))
    .query(async ({ input }) => { ... })
}
```

## 1.5 خطة التنفيذ

### المرحلة 1: البنية التحتية (أسبوع 1-2)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| إنشاء هيكل الملفات | 2 يوم | مجلدات ومكونات أساسية |
| تطوير API endpoints | 3 أيام | 10+ endpoints |
| إنشاء مكونات UI الأساسية | 3 أيام | KPICard, ChartContainer, etc. |
| إعداد نظام الفلاتر | 2 يوم | DateRangePicker, BranchSelector |

### المرحلة 2: لوحات التحكم (أسبوع 3-4)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| لوحة الملخص التنفيذي | 3 أيام | Executive Overview |
| لوحة المبيعات | 3 أيام | Sales Dashboard |
| لوحة المخزون | 2 يوم | Inventory Dashboard |
| لوحة المالية | 2 يوم | Financial Dashboard |

### المرحلة 3: التحسينات (أسبوع 5)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| تخصيص لوحة التحكم | 2 يوم | Drag & Drop Layout |
| تصدير التقارير | 2 يوم | PDF, Excel Export |
| تحسين الأداء | 1 يوم | Caching, Optimization |

---

# الجزء الثاني: منشئ التقارير المخصصة

## 2.1 الرؤية والأهداف

### الرؤية
تمكين المستخدمين من إنشاء تقاريرهم الخاصة بدون الحاجة لمعرفة تقنية، مع إمكانية حفظها وجدولتها وتصديرها.

### الأهداف

| الهدف | الوصف |
|-------|-------|
| سهولة الاستخدام | واجهة سحب وإفلات بسيطة |
| المرونة | دعم جميع مصادر البيانات |
| الأتمتة | جدولة التقارير وإرسالها تلقائيًا |
| التصدير | دعم PDF, Excel, CSV |

## 2.2 مكونات منشئ التقارير

### 2.2.1 واجهة المستخدم

```
┌─────────────────────────────────────────────────────────────────┐
│  📊 منشئ التقارير                              [حفظ] [معاينة]  │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────────────────────────────────┐  │
│  │ مصادر       │  │                                         │  │
│  │ البيانات    │  │         منطقة بناء التقرير              │  │
│  │             │  │                                         │  │
│  │ □ المبيعات  │  │  ┌─────────────────────────────────┐   │  │
│  │ □ المشتريات │  │  │     اسحب الحقول هنا            │   │  │
│  │ □ المخزون   │  │  └─────────────────────────────────┘   │  │
│  │ □ الموظفين  │  │                                         │  │
│  │ □ العملاء   │  │  الأعمدة المحددة:                       │  │
│  │             │  │  [التاريخ] [الفرع] [المبلغ] [+]        │  │
│  ├─────────────┤  │                                         │  │
│  │ الحقول      │  │  الفلاتر:                               │  │
│  │ المتاحة     │  │  [التاريخ >= 01/01/2026]               │  │
│  │             │  │  [الفرع = الرياض]                       │  │
│  │ • التاريخ   │  │                                         │  │
│  │ • الفرع     │  │  التجميع:                               │  │
│  │ • المبلغ    │  │  [تجميع حسب: الفرع]                     │  │
│  │ • العميل    │  │  [الترتيب: المبلغ تنازلي]              │  │
│  │ • المنتج    │  │                                         │  │
│  └─────────────┘  └─────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  معاينة النتائج:                                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ الفرع    │ إجمالي المبيعات │ عدد الفواتير │ المتوسط    │   │
│  │ الرياض  │ 50,000 ر.س      │ 150          │ 333 ر.س    │   │
│  │ جدة     │ 35,000 ر.س      │ 120          │ 291 ر.س    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2.2 مصادر البيانات المدعومة

| المصدر | الحقول المتاحة | العمليات المدعومة |
|--------|----------------|-------------------|
| **المبيعات** | التاريخ، الفرع، العميل، المنتج، الكمية، المبلغ، الحالة | SUM, AVG, COUNT, GROUP BY |
| **المشتريات** | التاريخ، المورد، المنتج، الكمية، التكلفة، الحالة | SUM, AVG, COUNT, GROUP BY |
| **المخزون** | المنتج، الفئة، الكمية، التكلفة، سعر البيع | SUM, AVG, MIN, MAX |
| **الموظفين** | الاسم، الفرع، الراتب، البونص، الحضور | SUM, AVG, COUNT |
| **المصاريف** | التاريخ، الفرع، التصنيف، المبلغ، الحالة | SUM, AVG, COUNT, GROUP BY |

### 2.2.3 أنواع التقارير

1. **تقارير جدولية** - عرض البيانات في جدول مع إمكانية الفرز والتصفية
2. **تقارير ملخصة** - تجميع البيانات مع إحصائيات
3. **تقارير مقارنة** - مقارنة فترات أو فروع
4. **تقارير رسومية** - رسوم بيانية تفاعلية

## 2.3 هيكل قاعدة البيانات

### جداول جديدة مطلوبة

```sql
-- جدول التقارير المحفوظة
CREATE TABLE saved_reports (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(200) NOT NULL,
  description TEXT,
  created_by INT NOT NULL,
  config JSON NOT NULL,  -- إعدادات التقرير
  is_public BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (created_by) REFERENCES users(id)
);

-- جدول جدولة التقارير
CREATE TABLE report_schedules (
  id INT PRIMARY KEY AUTO_INCREMENT,
  report_id INT NOT NULL,
  schedule_type ENUM('daily', 'weekly', 'monthly') NOT NULL,
  schedule_time TIME NOT NULL,
  schedule_day INT,  -- يوم الأسبوع أو الشهر
  recipients JSON NOT NULL,  -- قائمة البريد الإلكتروني
  format ENUM('pdf', 'excel', 'csv') DEFAULT 'pdf',
  is_active BOOLEAN DEFAULT TRUE,
  last_run TIMESTAMP,
  next_run TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (report_id) REFERENCES saved_reports(id)
);

-- جدول سجل تشغيل التقارير
CREATE TABLE report_execution_logs (
  id INT PRIMARY KEY AUTO_INCREMENT,
  report_id INT NOT NULL,
  executed_by INT,
  execution_type ENUM('manual', 'scheduled') NOT NULL,
  status ENUM('success', 'failed') NOT NULL,
  duration_ms INT,
  rows_returned INT,
  error_message TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (report_id) REFERENCES saved_reports(id)
);
```

## 2.4 هيكل إعدادات التقرير (JSON Config)

```typescript
interface ReportConfig {
  // مصدر البيانات
  dataSource: 'sales' | 'purchases' | 'inventory' | 'employees' | 'expenses';
  
  // الأعمدة المحددة
  columns: {
    field: string;
    label: string;
    type: 'text' | 'number' | 'date' | 'currency';
    aggregate?: 'sum' | 'avg' | 'count' | 'min' | 'max';
    format?: string;
  }[];
  
  // الفلاتر
  filters: {
    field: string;
    operator: 'eq' | 'neq' | 'gt' | 'gte' | 'lt' | 'lte' | 'contains' | 'between';
    value: any;
  }[];
  
  // التجميع
  groupBy?: string[];
  
  // الترتيب
  orderBy?: {
    field: string;
    direction: 'asc' | 'desc';
  }[];
  
  // الحدود
  limit?: number;
  
  // نوع العرض
  displayType: 'table' | 'chart' | 'summary';
  
  // إعدادات الرسم البياني (إذا كان chart)
  chartConfig?: {
    type: 'bar' | 'line' | 'pie' | 'area';
    xAxis: string;
    yAxis: string;
    series?: string;
  };
}
```

## 2.5 خطة التنفيذ

### المرحلة 1: البنية الأساسية (أسبوع 1-2)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| إنشاء جداول قاعدة البيانات | 1 يوم | 3 جداول جديدة |
| تطوير Query Builder | 3 أيام | محرك بناء الاستعلامات |
| إنشاء API endpoints | 2 يوم | CRUD للتقارير |
| واجهة اختيار مصدر البيانات | 2 يوم | Data Source Selector |

### المرحلة 2: واجهة البناء (أسبوع 3-4)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| واجهة سحب وإفلات | 3 أيام | Drag & Drop Builder |
| لوحة الفلاتر | 2 يوم | Filter Panel |
| معاينة النتائج | 2 يوم | Preview Component |
| حفظ واسترجاع التقارير | 3 أيام | Save/Load Functionality |

### المرحلة 3: التصدير والجدولة (أسبوع 5)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| تصدير PDF | 2 يوم | PDF Export |
| تصدير Excel | 1 يوم | Excel Export |
| جدولة التقارير | 2 يوم | Scheduling System |

---

# الجزء الثالث: تحليلات الذكاء الاصطناعي

## 3.1 الرؤية والأهداف

### الرؤية
دمج قدرات الذكاء الاصطناعي لتوفير رؤى تنبؤية وتوصيات ذكية تساعد في اتخاذ قرارات أعمال أفضل.

### الأهداف

| الهدف | الوصف | المقياس |
|-------|-------|---------|
| التنبؤ بالمبيعات | توقع المبيعات المستقبلية | دقة > 85% |
| اكتشاف الأنماط | تحديد أنماط الشراء والسلوك | تغطية > 90% |
| التوصيات الذكية | اقتراحات لتحسين الأداء | قابلية التنفيذ > 70% |
| الكشف عن الشذوذ | تحديد المعاملات غير الطبيعية | دقة > 95% |

## 3.2 حالات الاستخدام

### 3.2.1 التنبؤ بالمبيعات (Sales Forecasting)

**الوصف:** استخدام البيانات التاريخية للتنبؤ بالمبيعات المستقبلية.

**المدخلات:**
- بيانات المبيعات التاريخية (سنة على الأقل)
- الموسمية (رمضان، الأعياد، المناسبات)
- العروض والخصومات السابقة

**المخرجات:**
- توقع المبيعات للأسبوع/الشهر القادم
- نطاق الثقة (Confidence Interval)
- العوامل المؤثرة

**مثال على الاستجابة:**

```json
{
  "forecast": {
    "nextWeek": {
      "predicted": 45000,
      "lowerBound": 40000,
      "upperBound": 50000,
      "confidence": 0.85
    },
    "factors": [
      { "factor": "موسم الصيف", "impact": "+15%" },
      { "factor": "عرض خاص", "impact": "+10%" },
      { "factor": "يوم الخميس", "impact": "+20%" }
    ]
  }
}
```

### 3.2.2 تحليل سلوك العملاء (Customer Behavior Analysis)

**الوصف:** فهم أنماط شراء العملاء وتقسيمهم إلى شرائح.

**التحليلات:**

| التحليل | الوصف | الفائدة |
|---------|-------|---------|
| RFM Analysis | Recency, Frequency, Monetary | تصنيف العملاء |
| Churn Prediction | توقع فقدان العملاء | الاحتفاظ بالعملاء |
| CLV Prediction | قيمة العميل مدى الحياة | تحديد الأولويات |
| Basket Analysis | تحليل سلة المشتريات | البيع المتقاطع |

**مثال على تقسيم العملاء:**

```
┌────────────────────────────────────────────────────────┐
│  تصنيف العملاء (RFM Analysis)                         │
├────────────────────────────────────────────────────────┤
│  🏆 Champions (15%)     - عملاء ممتازون، حافظ عليهم   │
│  💎 Loyal (25%)         - عملاء مخلصون، كافئهم        │
│  🌟 Potential (20%)     - إمكانية عالية، طورهم        │
│  😴 At Risk (15%)       - معرضون للفقدان، تواصل معهم  │
│  💤 Hibernating (25%)   - غير نشطين، أعد تنشيطهم      │
└────────────────────────────────────────────────────────┘
```

### 3.2.3 تحسين المخزون (Inventory Optimization)

**الوصف:** توصيات ذكية لإدارة المخزون.

**التحليلات:**
- **توقع الطلب:** كمية المنتج المتوقع بيعها
- **نقطة إعادة الطلب:** متى يجب طلب المنتج
- **الكمية المثلى:** كم يجب طلبه (EOQ)
- **المنتجات البطيئة:** منتجات لا تتحرك

### 3.2.4 الكشف عن الشذوذ (Anomaly Detection)

**الوصف:** تحديد المعاملات أو الأنماط غير الطبيعية.

**أنواع الشذوذ:**

| النوع | المثال | الإجراء |
|-------|--------|---------|
| مبيعات غير طبيعية | فاتورة بقيمة 10x المتوسط | مراجعة وتأكيد |
| مصاريف مشبوهة | مصروف كبير غير معتاد | تحقيق |
| أنماط غريبة | مبيعات في أوقات غير معتادة | تنبيه |
| تغييرات مفاجئة | انخفاض حاد في المبيعات | تحليل السبب |

### 3.2.5 التوصيات الذكية (Smart Recommendations)

**أنواع التوصيات:**

1. **توصيات التسعير**
   - "زيادة سعر المنتج X بنسبة 5% لن تؤثر على المبيعات"
   - "تخفيض سعر المنتج Y سيزيد المبيعات 20%"

2. **توصيات المخزون**
   - "اطلب 100 وحدة من المنتج Z خلال أسبوع"
   - "المنتج W بطيء الحركة، ضع عرض عليه"

3. **توصيات التسويق**
   - "العميل أحمد لم يزر منذ 30 يوم، أرسل له عرض"
   - "أفضل وقت للعروض: الخميس والجمعة"

4. **توصيات الموظفين**
   - "الموظف X يحتاج تدريب على المنتجات الجديدة"
   - "فرع Y يحتاج موظف إضافي أيام الخميس"

## 3.3 التنفيذ التقني

### 3.3.1 استخدام LLM المدمج

النظام يحتوي على `invokeLLM` جاهز للاستخدام:

```typescript
// server/ai/analyticsEngine.ts

import { invokeLLM } from "../_core/llm";

export async function analyzeSalesData(salesData: SalesData[]) {
  const prompt = `
    أنت محلل بيانات خبير. قم بتحليل بيانات المبيعات التالية وقدم:
    1. ملخص الأداء
    2. الأنماط الملاحظة
    3. التوصيات
    
    البيانات:
    ${JSON.stringify(salesData)}
  `;

  const response = await invokeLLM({
    messages: [
      { role: "system", content: "أنت محلل بيانات متخصص في تحليل أداء الأعمال." },
      { role: "user", content: prompt }
    ],
    response_format: {
      type: "json_schema",
      json_schema: {
        name: "sales_analysis",
        strict: true,
        schema: {
          type: "object",
          properties: {
            summary: { type: "string" },
            patterns: { type: "array", items: { type: "string" } },
            recommendations: { type: "array", items: { type: "string" } },
            riskLevel: { type: "string", enum: ["low", "medium", "high"] }
          },
          required: ["summary", "patterns", "recommendations", "riskLevel"]
        }
      }
    }
  });

  return JSON.parse(response.choices[0].message.content);
}
```

### 3.3.2 خدمات التحليل

```typescript
// server/ai/services/

// 1. خدمة التنبؤ
export class ForecastingService {
  async predictSales(branchId: number, days: number): Promise<Forecast>;
  async predictInventoryDemand(productId: number): Promise<DemandForecast>;
}

// 2. خدمة تحليل العملاء
export class CustomerAnalyticsService {
  async segmentCustomers(): Promise<CustomerSegments>;
  async predictChurn(customerId: number): Promise<ChurnPrediction>;
  async calculateCLV(customerId: number): Promise<number>;
}

// 3. خدمة الكشف عن الشذوذ
export class AnomalyDetectionService {
  async detectSalesAnomalies(dateRange: DateRange): Promise<Anomaly[]>;
  async detectExpenseAnomalies(dateRange: DateRange): Promise<Anomaly[]>;
}

// 4. خدمة التوصيات
export class RecommendationService {
  async getPricingRecommendations(): Promise<PricingRecommendation[]>;
  async getInventoryRecommendations(): Promise<InventoryRecommendation[]>;
  async getMarketingRecommendations(): Promise<MarketingRecommendation[]>;
}
```

### 3.3.3 واجهة المستخدم

```
┌─────────────────────────────────────────────────────────────────┐
│  🤖 التحليلات الذكية                                           │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 💡 رؤى اليوم                                            │   │
│  │                                                         │   │
│  │ • المبيعات أعلى من المتوقع بـ 15% 📈                    │   │
│  │ • 3 عملاء معرضون لخطر الفقدان ⚠️                        │   │
│  │ • 5 منتجات تحتاج إعادة طلب 📦                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📊 توقعات الأسبوع القادم                                │   │
│  │                                                         │   │
│  │ [رسم بياني للتوقعات مع نطاق الثقة]                      │   │
│  │                                                         │   │
│  │ المتوقع: 45,000 ر.س (±5,000)                            │   │
│  │ الثقة: 85%                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🎯 التوصيات                                             │   │
│  │                                                         │   │
│  │ 1. [عالية] اتصل بالعميل "أحمد" - لم يزر منذ 45 يوم     │   │
│  │ 2. [متوسطة] اطلب 50 وحدة من "شامبو X" خلال 3 أيام      │   │
│  │ 3. [منخفضة] ضع عرض على "كريم Y" - بطيء الحركة          │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 3.4 خطة التنفيذ

### المرحلة 1: البنية الأساسية (أسبوع 1-2)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| إنشاء خدمات التحليل | 3 أيام | 4 خدمات أساسية |
| تطوير Prompts للـ LLM | 2 يوم | قوالب التحليل |
| إنشاء API endpoints | 2 يوم | 10+ endpoints |
| جداول تخزين النتائج | 1 يوم | 3 جداول جديدة |

### المرحلة 2: التحليلات الأساسية (أسبوع 3-4)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| التنبؤ بالمبيعات | 3 أيام | Sales Forecasting |
| تحليل العملاء (RFM) | 2 يوم | Customer Segmentation |
| الكشف عن الشذوذ | 3 أيام | Anomaly Detection |
| واجهة المستخدم | 2 يوم | AI Insights Dashboard |

### المرحلة 3: التوصيات الذكية (أسبوع 5-6)

| المهمة | المدة | المخرجات |
|--------|-------|----------|
| توصيات المخزون | 2 يوم | Inventory Recommendations |
| توصيات التسويق | 2 يوم | Marketing Recommendations |
| توصيات التسعير | 2 يوم | Pricing Recommendations |
| نظام الإشعارات الذكية | 2 يوم | Smart Alerts |
| اختبار وتحسين | 2 يوم | Testing & Optimization |

---

# الجزء الرابع: الخطة الزمنية الشاملة

## 4.1 الجدول الزمني

```
الأسبوع 1-2:   [████████████████████] البنية التحتية لـ BI
الأسبوع 3-4:   [████████████████████] لوحات التحكم
الأسبوع 5:     [██████████] تحسينات BI + بداية Report Builder
الأسبوع 6-7:   [████████████████████] منشئ التقارير
الأسبوع 8:     [██████████] التصدير والجدولة
الأسبوع 9-10:  [████████████████████] تحليلات AI الأساسية
الأسبوع 11-12: [████████████████████] التوصيات الذكية + الاختبار
```

## 4.2 الموارد المطلوبة

| المورد | الكمية | الملاحظات |
|--------|--------|-----------|
| مطور Frontend | 1 | React, TypeScript, Recharts |
| مطور Backend | 1 | Node.js, tRPC, Drizzle |
| مطور AI/ML | 0.5 | Prompt Engineering |
| مصمم UI/UX | 0.5 | تصميم لوحات التحكم |

## 4.3 المخاطر والتخفيف

| المخاطر | الاحتمالية | التأثير | التخفيف |
|---------|------------|---------|---------|
| بطء أداء الاستعلامات | متوسط | عالي | Caching, Indexing |
| عدم دقة التنبؤات | متوسط | متوسط | تحسين مستمر للـ Prompts |
| تعقيد واجهة المستخدم | منخفض | متوسط | اختبار المستخدمين |
| تكلفة استدعاءات LLM | منخفض | منخفض | Caching, Rate Limiting |

## 4.4 معايير النجاح

| المعيار | المستهدف | طريقة القياس |
|---------|----------|--------------|
| وقت تحميل لوحة التحكم | < 3 ثواني | Performance Monitoring |
| دقة التنبؤات | > 85% | مقارنة بالفعلي |
| رضا المستخدمين | > 4/5 | استبيانات |
| استخدام التقارير المخصصة | > 50% من المستخدمين | Analytics |

---

## الخلاصة

تقدم هذه الخطة رؤية شاملة لتطوير نظام تحليلات متقدم يشمل:

1. **لوحة BI متقدمة** توفر رؤية شاملة للأعمال مع رسوم بيانية تفاعلية
2. **منشئ تقارير مخصصة** يمكّن المستخدمين من إنشاء تقاريرهم بدون مساعدة تقنية
3. **تحليلات ذكاء اصطناعي** تقدم رؤى تنبؤية وتوصيات قابلة للتنفيذ

المدة الإجمالية المقدرة: **12 أسبوع** (3 أشهر)

---

*تم إعداد هذه الخطة بواسطة Manus AI*
