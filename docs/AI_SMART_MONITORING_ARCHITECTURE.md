# نظام المراقبة الذكية المتقدم - Symbol AI ERP

## 🎯 الهدف
نظام مراقبة ذكي شامل يستخدم Claude/GPT للتحليل العميق مع منطق تفكير واضح ومحسوب لكل خطوة.

---

## 🏗️ المعمارية العامة

```
┌─────────────────────────────────────────────────────────────────┐
│                    نظام ERP - Symbol AI                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │   إيرادات    │    │   مصاريف    │    │   طلبات     │      │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘      │
│         │                   │                   │               │
│         ▼                   ▼                   ▼               │
│  ┌─────────────────────────────────────────────────────┐       │
│  │              🔔 نظام Triggers                       │       │
│  │  ─────────────────────────────────────────────────  │       │
│  │  • onRevenueCreated    • onExpenseCreated          │       │
│  │  • onMismatchDetected  • onDailyReport             │       │
│  │  • onWeeklyReport      • onJustificationRequired   │       │
│  └──────────────────────────┬──────────────────────────┘       │
│                             │                                   │
│                             ▼                                   │
│  ┌─────────────────────────────────────────────────────┐       │
│  │           🧠 محرك التحليل الذكي (AI Engine)         │       │
│  │  ─────────────────────────────────────────────────  │       │
│  │                                                     │       │
│  │  ┌─────────────────────────────────────────────┐   │       │
│  │  │     Claude 4.5 Sonnet / GPT-5.1             │   │       │
│  │  │     (عبر invokeLLM المدمج)                  │   │       │
│  │  └─────────────────────────────────────────────┘   │       │
│  │                                                     │       │
│  │  Chain of Thought Analysis:                        │       │
│  │  1️⃣ جمع السياق والبيانات التاريخية                │       │
│  │  2️⃣ تحليل الأنماط والمقارنات                      │       │
│  │  3️⃣ تحديد الانحرافات والشذوذ                      │       │
│  │  4️⃣ تقييم مستوى الخطورة                          │       │
│  │  5️⃣ توليد الأسئلة أو التوصيات                     │       │
│  │  6️⃣ صياغة التقرير النهائي                        │       │
│  └──────────────────────────┬──────────────────────────┘       │
│                             │                                   │
│                             ▼                                   │
│  ┌─────────────────────────────────────────────────────┐       │
│  │              📤 نظام الإخراج والتصدير               │       │
│  │  ─────────────────────────────────────────────────  │       │
│  │                                                     │       │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐            │       │
│  │  │ Resend  │  │ Zapier  │  │Database │            │       │
│  │  │ (Email) │  │ (Sheets)│  │ (Logs)  │            │       │
│  │  └─────────┘  └─────────┘  └─────────┘            │       │
│  └─────────────────────────────────────────────────────┘       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🧠 منطق التفكير (Chain of Thought)

### السيناريو 1: تحليل إيراد منخفض

```typescript
// المدخلات
const input = {
  revenue: 300,           // الإيراد المسجل
  branch: "طويق",
  date: "2024-12-21",
  dayOfWeek: "السبت"
};

// Chain of Thought - خطوات التفكير
const chainOfThought = {
  
  // الخطوة 1: جمع السياق
  step1_context: {
    action: "جمع البيانات التاريخية",
    data: {
      averageRevenue: 8500,           // متوسط إيراد هذا الفرع
      saturdayAverage: 9200,          // متوسط أيام السبت
      lastWeekSameDay: 8800,          // السبت الماضي
      last30DaysMin: 5500,            // أقل إيراد في 30 يوم
      last30DaysMax: 12000            // أعلى إيراد في 30 يوم
    }
  },

  // الخطوة 2: تحليل الانحراف
  step2_deviation: {
    action: "حساب نسبة الانحراف",
    calculations: {
      deviationFromAverage: -96.5,    // (300-8500)/8500 * 100
      deviationFromSaturday: -96.7,   // (300-9200)/9200 * 100
      deviationFromLastWeek: -96.6,   // (300-8800)/8800 * 100
      isHistoricalLow: true           // أقل من أي يوم في 30 يوم
    }
  },

  // الخطوة 3: تقييم الخطورة
  step3_severity: {
    action: "تحديد مستوى الخطورة",
    rules: [
      { condition: "انحراف > 50%", severity: "عالي", matched: true },
      { condition: "أقل من الحد الأدنى التاريخي", severity: "حرج", matched: true },
      { condition: "يوم ذروة (سبت)", severity: "+1 مستوى", matched: true }
    ],
    finalSeverity: "حرج",
    severityScore: 95  // من 100
  },

  // الخطوة 4: توليد الفرضيات
  step4_hypotheses: {
    action: "توليد الأسباب المحتملة",
    hypotheses: [
      { reason: "الفرع مغلق", probability: 40 },
      { reason: "مشكلة في نظام الكاشير", probability: 25 },
      { reason: "خطأ في الإدخال", probability: 20 },
      { reason: "ظروف طارئة", probability: 15 }
    ]
  },

  // الخطوة 5: توليد الأسئلة
  step5_questions: {
    action: "صياغة أسئلة للمشرف",
    questions: [
      "هل كان الفرع مفتوحاً طوال ساعات العمل؟",
      "هل واجهتم مشاكل تقنية في الكاشير أو الشبكة؟",
      "هل تم تسجيل جميع المبيعات في النظام؟",
      "هل هناك ظروف خاصة أثرت على العمل؟"
    ],
    deadline: "24 ساعة",
    escalationPath: ["مشرف الفرع", "المشرف العام", "الأدمن"]
  },

  // الخطوة 6: القرار النهائي
  step6_decision: {
    action: "تحديد الإجراء المطلوب",
    decision: "طلب مبررات عاجل",
    notifications: [
      { recipient: "مشرف طويق", type: "طلب مبررات", priority: "عاجل" },
      { recipient: "المشرف العام", type: "نسخة للعلم", priority: "عالي" },
      { recipient: "الأدمن", type: "نسخة للعلم", priority: "عالي" }
    ],
    logging: {
      table: "ai_analysis_logs",
      severity: "critical",
      requiresFollowup: true
    }
  }
};
```

---

### السيناريو 2: تحليل مصروف مرتفع

```typescript
const expenseAnalysis = {
  
  // المدخلات
  input: {
    expense: 3500,
    category: "صيانة",
    branch: "لبن",
    description: "صيانة مكيفات"
  },

  // Chain of Thought
  step1_context: {
    historicalData: {
      avgMaintenanceExpense: 800,
      lastMonthMaintenance: 1200,
      yearlyMaintenanceBudget: 15000,
      yearlyMaintenanceSpent: 9500
    }
  },

  step2_analysis: {
    deviationFromAvg: 337.5,          // %
    percentOfBudget: 23.3,            // % من الميزانية السنوية
    remainingBudget: 5500,
    monthsRemaining: 6
  },

  step3_patterns: {
    isRecurring: false,               // ليس مصروف متكرر
    seasonalFactor: 1.3,              // الصيف = مصاريف مكيفات أعلى
    adjustedDeviation: 259.6          // بعد تعديل الموسمية
  },

  step4_riskAssessment: {
    budgetRisk: "متوسط",              // 23% من الميزانية
    frequencyRisk: "منخفض",           // غير متكرر
    amountRisk: "عالي",               // 4x المتوسط
    overallRisk: "متوسط-عالي"
  },

  step5_questions: [
    "هل تم الحصول على عروض أسعار متعددة؟",
    "هل هذه صيانة وقائية أم طارئة؟",
    "هل يوجد عقد صيانة سنوي يغطي هذا؟",
    "ما هو العمر الافتراضي المتبقي للمكيفات؟"
  ],

  step6_recommendation: {
    action: "طلب توضيح",
    priority: "متوسط",
    suggestion: "النظر في عقد صيانة سنوي لتقليل التكاليف"
  }
};
```

---

### السيناريو 3: التقرير الأسبوعي الذكي

```typescript
const weeklyReportAnalysis = {
  
  // جمع البيانات
  dataCollection: {
    period: "15-21 ديسمبر 2024",
    branches: ["طويق", "لبن"],
    metrics: {
      طويق: { revenue: 52000, expenses: 8500, profit: 43500 },
      لبن: { revenue: 48000, expenses: 7200, profit: 40800 }
    }
  },

  // تحليل الأداء
  performanceAnalysis: {
    طويق: {
      vsLastWeek: +8.3,               // %
      vsLastMonth: +12.1,
      trend: "صاعد",
      bestDay: { day: "الخميس", revenue: 12000 },
      worstDay: { day: "الأحد", revenue: 5500 }
    },
    لبن: {
      vsLastWeek: -3.2,
      vsLastMonth: +5.4,
      trend: "مستقر مع تراجع طفيف",
      bestDay: { day: "الجمعة", revenue: 11000 },
      worstDay: { day: "الاثنين", revenue: 4800 }
    }
  },

  // تحليل الأنماط
  patternAnalysis: {
    peakDays: ["الخميس", "الجمعة"],
    lowDays: ["الأحد", "الاثنين"],
    seasonalTrend: "موسم الشتاء - طلب متوسط",
    anomalies: [
      { date: "17 ديسمبر", branch: "لبن", type: "إيراد منخفض", resolved: true }
    ]
  },

  // التوقعات
  predictions: {
    nextWeekRevenue: {
      طويق: { min: 48000, expected: 54000, max: 60000 },
      لبن: { min: 45000, expected: 49000, max: 53000 }
    },
    confidence: 78,                   // %
    factors: ["اقتراب نهاية السنة", "عطلة رأس السنة"]
  },

  // التوصيات
  recommendations: [
    {
      priority: "عالي",
      branch: "لبن",
      recommendation: "مراجعة سبب التراجع الطفيف",
      expectedImpact: "+5% إيرادات"
    },
    {
      priority: "متوسط",
      branch: "طويق",
      recommendation: "زيادة الموظفين يوم الخميس",
      expectedImpact: "+8% إيرادات يوم الخميس"
    },
    {
      priority: "منخفض",
      branch: "الكل",
      recommendation: "الاستعداد لموسم رأس السنة",
      expectedImpact: "تجنب نقص المخزون"
    }
  ],

  // الملخص التنفيذي
  executiveSummary: `
    📊 ملخص الأسبوع (15-21 ديسمبر)
    
    ✅ الأداء العام: جيد
    💰 إجمالي الإيرادات: 100,000 ر.س. (+3.2%)
    📈 فرع طويق: أداء ممتاز (+8.3%)
    ⚠️ فرع لبن: تراجع طفيف (-3.2%)
    
    🎯 التوصية الرئيسية:
    مراجعة أداء فرع لبن وتحديد أسباب التراجع
    
    📅 توقع الأسبوع القادم: 103,000 ر.س.
  `
};
```

---

## 🔔 نظام Triggers

```typescript
// تعريف الـ Triggers
const triggers = {
  
  // Trigger 1: عند إدخال إيراد
  onRevenueCreated: {
    conditions: [
      { type: "threshold", field: "amount", operator: "<", value: 500, action: "lowRevenueAlert" },
      { type: "threshold", field: "amount", operator: "<", value: "dailyAverage * 0.3", action: "criticalRevenueAlert" },
      { type: "mismatch", field: "isMatching", value: false, action: "mismatchAlert" }
    ],
    actions: {
      lowRevenueAlert: {
        analyze: true,
        notify: ["branchSupervisor", "generalSupervisor", "admin"],
        requestJustification: true,
        deadline: "24h"
      },
      criticalRevenueAlert: {
        analyze: true,
        notify: ["branchSupervisor", "generalSupervisor", "admin"],
        requestJustification: true,
        deadline: "4h",
        escalate: true
      },
      mismatchAlert: {
        analyze: true,
        notify: ["branchSupervisor", "generalSupervisor", "admin"],
        requestJustification: true,
        deadline: "24h"
      }
    }
  },

  // Trigger 2: عند إدخال مصروف
  onExpenseCreated: {
    conditions: [
      { type: "threshold", field: "amount", operator: ">", value: 500, action: "highExpenseAlert" },
      { type: "threshold", field: "amount", operator: ">", value: "categoryAverage * 3", action: "unusualExpenseAlert" }
    ],
    actions: {
      highExpenseAlert: {
        analyze: true,
        notify: ["branchSupervisor", "generalSupervisor", "admin"],
        requestClarification: true
      },
      unusualExpenseAlert: {
        analyze: true,
        notify: ["generalSupervisor", "admin"],
        requestJustification: true,
        requireApproval: true
      }
    }
  },

  // Trigger 3: التقرير اليومي
  onDailyReport: {
    schedule: "0 22 * * *",           // كل يوم الساعة 10 مساءً
    actions: {
      generateReport: true,
      analyzeDay: true,
      compareWithHistory: true,
      sendEmail: ["admin", "generalSupervisor"],
      exportToSheets: true
    }
  },

  // Trigger 4: التقرير الأسبوعي
  onWeeklyReport: {
    schedule: "0 10 * * 0",           // كل أحد الساعة 10 صباحاً
    actions: {
      generateReport: true,
      deepAnalysis: true,
      predictions: true,
      recommendations: true,
      sendEmail: ["admin", "generalSupervisor"],
      exportToSheets: true
    }
  },

  // Trigger 5: متابعة المبررات
  onJustificationFollowup: {
    schedule: "0 */4 * * *",          // كل 4 ساعات
    conditions: [
      { type: "pending", field: "justificationStatus", value: "pending", age: ">24h" }
    ],
    actions: {
      escalate: true,
      sendReminder: true,
      notifyHigherLevel: true
    }
  }
};
```

---

## 📊 التصدير التلقائي مع Zapier

```typescript
// تكامل Zapier
const zapierIntegration = {
  
  // تصدير الإيرادات
  exportRevenues: {
    trigger: "onRevenueCreated",
    destination: "Google Sheets",
    spreadsheetId: "1abc...xyz",
    worksheet: "الإيرادات اليومية",
    mapping: {
      "التاريخ": "date",
      "الفرع": "branchName",
      "إيراد نقدي": "cashRevenue",
      "إيراد شبكة": "networkRevenue",
      "إيراد مدى": "madaRevenue",
      "المجموع": "totalRevenue",
      "الموازنة": "balanceStatus",
      "التحليل": "aiAnalysis"
    }
  },

  // تصدير المصاريف
  exportExpenses: {
    trigger: "onExpenseApproved",
    destination: "Google Sheets",
    spreadsheetId: "1abc...xyz",
    worksheet: "المصاريف المعتمدة",
    mapping: {
      "التاريخ": "date",
      "الفرع": "branchName",
      "التصنيف": "category",
      "المبلغ": "amount",
      "الوصف": "description",
      "الحالة": "status",
      "التحليل": "aiAnalysis"
    }
  },

  // إرسال التقارير
  sendReports: {
    daily: {
      trigger: "onDailyReport",
      destination: "Gmail",
      recipients: ["Nntn127@gmail.com", "Salemalwadai1997@gmail.com"],
      template: "daily_report_template"
    },
    weekly: {
      trigger: "onWeeklyReport",
      destination: "Gmail",
      recipients: ["Nntn127@gmail.com", "Salemalwadai1997@gmail.com"],
      template: "weekly_report_template"
    }
  },

  // طلبات المبررات
  justificationRequests: {
    trigger: "onJustificationRequired",
    destination: "Gmail",
    recipients: "dynamic",            // حسب الفرع
    template: "justification_request_template",
    trackResponses: true
  }
};
```

---

## 📁 هيكل الملفات

```
server/
├── ai/
│   ├── analysisEngine.ts           # محرك التحليل الذكي
│   ├── chainOfThought.ts           # منطق التفكير المتسلسل
│   ├── prompts/
│   │   ├── revenueAnalysis.ts      # قوالب تحليل الإيرادات
│   │   ├── expenseAnalysis.ts      # قوالب تحليل المصاريف
│   │   ├── weeklyReport.ts         # قوالب التقرير الأسبوعي
│   │   └── justificationEval.ts    # قوالب تقييم المبررات
│   └── types.ts                    # أنواع TypeScript
├── triggers/
│   ├── triggerManager.ts           # مدير الـ Triggers
│   ├── revenuesTriggers.ts         # triggers الإيرادات
│   ├── expensesTriggers.ts         # triggers المصاريف
│   └── reportsTriggers.ts          # triggers التقارير
├── integrations/
│   ├── zapierExport.ts             # تصدير Zapier
│   └── sheetsSync.ts               # مزامنة Google Sheets
└── monitoring/
    ├── smartMonitor.ts             # المراقب الذكي الشامل
    └── alertManager.ts             # مدير التنبيهات
```

---

## 🚀 الخطوات التالية

1. **إنشاء محرك التحليل الذكي** مع Chain of Thought
2. **تنفيذ نظام Triggers** للأحداث المختلفة
3. **ربط مع Zapier** للتصدير التلقائي
4. **اختبار السيناريوهات** المختلفة
5. **تفعيل المراقبة** في الإنتاج
